# Verified working set. Python 3.12.14 on macOS 26.5.1 / arm64 (Apple M5).
#
# torch is installed FIRST and from default PyPI, not from a CUDA index.
# VGGT-World's own requirements.txt pins `--extra-index-url .../cu128`, which
# fetches CUDA wheels and fails on macOS -- see README, Apple Silicon notes.
#
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install torch==2.13.0 torchvision==0.28.0
#   pip install -r requirements.txt

torch==2.13.0
torchvision==0.28.0          # supplies raft_large for the flow baseline
numpy==2.5.2
opencv-python==4.11.0.86     # imported as cv2
scipy==1.18.1
safetensors==0.8.0           # loading facebook/VGGT-1B
Pillow==10.4.0

# KittiMoSeg ground truth only. Install into an isolated dir so they cannot
# disturb the runtime env:  pip install --target .tools gdown py7zr
# gdown==6.1.0
# py7zr==1.1.3
